# bookmark-manager

To do:

    Frontend	React.js + Tailwind CSS
    Backend	    Node.js (Express.js)
    Database	MySQL + IndexedDB
    Storage	    IndexedDB

Later to repurpose as chrome extension:

    Packaging	Webpack/Parcel

Development Plan:

    Offline-First Approach:

        Start with React.js and IndexedDB for offline functionality.
        Create bookmark CRUD operations stored locally.
    Sync Capability:

        Add Node.js and MySQL for optional syncing.
        Develop APIs for bookmark storage and retrieval.
    Chrome Extension:

        Adapt React components into a Chrome extension structure using a manifest.json.